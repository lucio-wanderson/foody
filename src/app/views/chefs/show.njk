{%extends "admin/layout.njk"%}

{%block content%}
    <h1>Chef: {{chef.name_chef}}</h1>

    <button id="editChef" class="add-button">Editar chef</button>

    <div class="chef-show">
        <div class="chef-image" style="background: url({{chef.src}}) no-repeat center center / cover">
        </div>
        <div>
            <h2>{{chef.name_chef}}</h2>
            <p>{{chef.total_recipes}} receitas</p>
        </div>
    </div>

    <div class="all-recipes">

        <div class="recipes"> 

            {% for recipe in recipes %}
                <div class="link-show" id="{{recipe.id_recipe}}">
                    <div class="recipe" style="background: url({{recipe.src}})no-repeat center center/cover"></div>
                    <div>
                        <h4>{{ recipe.title }}</h4>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>
{%endblock content%}

{%block script%}
    <script>
        const editChef = document.getElementById("editChef")
        editChef.addEventListener("click", function () {
            window.location.href = `/admin/chefs/{{chef.id_chef}}/edit`
        })
    </script>
{%endblock script%}