{%extends "layout.njk"%}

{%block content%}

        <div class="presentation">

            <div class="presentation-content">
                <h1>As melhores receitas</h1>
                <p>Aprenda a criar os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
            </div>
            <div class="presentation-img">
                <img src="/images/119236-OQ05QX-268.png" alt="">
            </div>

        </div>

        <div class="most-accessed">
            <h2>Receitas mais acessadas</h2>

            <div class="most-accessed-recipe">

                {% for recipe in recipes %}
                <div>
                    <div class="recipe" style="background: url({{recipe.image}})no-repeat center center/cover"></div>
                    <div>
                        <h4 class="title-recipe">{{ recipe.title }}</h4>
                        <p class="p-recipe">{{ recipe.chef_name }}</p>
                    </div>
                </div>
            {% endfor %}

            </div>
        </div>

    {%endblock%}